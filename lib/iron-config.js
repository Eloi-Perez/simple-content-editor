export const ironOptions = {
    cookieName: "simple_content_editor_login_cookie",
    password: process.env.IRON_SECRET,
    // cookieOptions: {
    //     maxAge: undefined,
    //     secure: process.env.NODE_ENV === "production", // True = HTTPS
    // },
}